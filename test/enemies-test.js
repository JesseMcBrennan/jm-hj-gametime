const chai = require('chai');
const assert = chai.assert;
const Frog = require('../lib/enemies.js');

describe('Enemy', function) {
  it('should have a name', function() {
    var enemy = new Enemy();

    assert.equal(enemy.name, name);
    assert.equal(enemy.name. name);
  })
});
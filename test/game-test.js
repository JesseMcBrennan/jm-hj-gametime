const chai = require('chai');
const assert = chai.assert;
const Game = require('../lib/game.js');

describe('Game', function() {
  it('should have characters', function() {
    var game = new Game();
    // var expectedHeight = 30;
    // var expected

    assert.equal(game.width, x);
  });
});

